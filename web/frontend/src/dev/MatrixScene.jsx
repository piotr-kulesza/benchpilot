// MatrixScene — animation harness (?matrix=1). Fleshed out in Phase 3 once the
// container/equipment CONTRACT (Phase 2) exists; for now it is a placeholder so the
// dev bundle builds during Phase 1 (model gallery).
export function MatrixScene() {
  return null
}
