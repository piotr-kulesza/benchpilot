{
  "name": "benchpilot-player",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "benchpilot — a bench-usable protocol player over the parsed schema",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.8",
    "vitest": "^2.1.2"
  }
}
