# Core runtime — the only hard dependency for a live parse.
anthropic>=0.40

# Optional: nicer .docx extraction. If absent, core/ingest.py falls back to a
# dependency-free unzip of word/document.xml, so this is not strictly required.
python-docx>=1.1

# Live-parse / voice backend (web/api.py) — also what Vercel serves as the API.
fastapi>=0.110
python-multipart>=0.0.9   # UploadFile parsing for /api/parse-file

# Test runner.
pytest>=8.0
