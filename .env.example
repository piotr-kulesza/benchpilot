# Copy this file to .env and fill in your key:  cp .env.example .env
# .env is gitignored; this template is committed.

# Required for a LIVE parse (scripts/parse_check.py, web/api.py). Cached parses
# and the offline tests need nothing. Get a key at https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...

# Optional: point the frontend at a running live-parse backend (web/api.py).
# VITE_API_BASE=http://localhost:8000
